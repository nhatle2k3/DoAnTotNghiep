# Copy to .env and adjust
PORT=4000
JWT_SECRET=supersecret_change_me
DATABASE_URL=postgres://postgres:postgres@localhost:5432/trinh_cafe
# Allowed origin for CORS. For development access from phone, set to your machine IP or use '*' (not recommended for production):
# e.g. ALLOW_ORIGIN=http://192.168.1.42:5173
ALLOW_ORIGIN=http://localhost:5173
QR_OUTPUT_DIR=./qr_codes

# Optional: when generating QR codes for local testing, set QR_HOST to the public URL
# that the QR should point to (e.g. http://192.168.1.42:4000)
# QR_HOST=http://localhost:4000
